# Environment Variables for Bot Game
# SECURITY CRITICAL: Copy this file to .env and FILL IN YOUR OWN VALUES
# Never commit .env to version control!

# =======================
# FLASK & DEPLOYMENT
# =======================
# Set to 'production' when deploying. Enables strict security checks.
FLASK_ENV=development

# SECURITY CRITICAL: Generate a unique, long secret key for production
# Generate with: python generate_secret.py
# Must be at least 32 random characters
SECRET_KEY=your-secure-secret-key-here-CHANGE-THIS-IN-PRODUCTION

# Port to run the application on
PORT=5000

# =======================
# SESSION SECURITY
# =======================
# CRITICAL: Must be True in production (requires HTTPS)
# Only False in development/localhost
SESSION_COOKIE_SECURE=False

# Prevents JavaScript from accessing session cookies (XSS protection)
SESSION_COOKIE_HTTPONLY=True

# CSRF protection: 'Lax' balances security and usability
SESSION_COOKIE_SAMESITE=Lax

# =======================
# CORS (Cross-Origin Access)
# =======================
# CRITICAL: Set to your actual domain(s) in production
# Default (localhost) is only safe for development
# Format: comma-separated list of allowed origins
# Examples:
#   Single domain:  ALLOWED_ORIGINS=https://myapp.com
#   Multiple:       ALLOWED_ORIGINS=https://myapp.com,https://www.myapp.com
ALLOWED_ORIGINS=http://localhost:5000

# =======================
# APPLICATION SETTINGS
# =======================
APP_NAME=Bot Game
APP_VERSION=1.0.0

# =======================
# SECURITY CHECKLIST
# =======================
# Before deploying to production, ensure:
# [ ] SECRET_KEY is set to a unique, random value (use generate_secret.py)
# [ ] FLASK_ENV=production
# [ ] SESSION_COOKIE_SECURE=True (requires HTTPS)
# [ ] ALLOWED_ORIGINS set to your domain (not localhost)
# [ ] Application is served over HTTPS
# [ ] .env file is in .gitignore
# [ ] .env file has restrictive file permissions (chmod 600)
# [ ] All dependencies are up to date
